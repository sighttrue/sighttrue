{
  "name": "readout",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Collector agent for a unified developer-signal instrument. See MASTER.md.",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.functions.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "seed:watchlist": "node scripts/seed-watchlist.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/ibm-plex-sans-condensed": "^5.3.0",
    "@fontsource/ibm-plex-serif": "^5.3.0",
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
