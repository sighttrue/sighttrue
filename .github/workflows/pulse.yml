name: Pulse

# Every four hours. The :17 offset avoids the top of the hour, which is the
# most congested window and where scheduled runs are most likely to be dropped.
#
# Four-hourly is the ceiling, not a preference: Cloudflare Pages allows 500
# builds a month and every data refresh needs a deployment. Six runs a day is
# 180. Hourly would be 720 and over the limit.
on:
  schedule:
    - cron: '17 */4 * * *'
  workflow_dispatch:
    inputs:
      limit:
        description: 'Collect only the first N repositories (dry run)'
        required: false
        type: string

permissions:
  contents: write
  issues: write

# Pulse and daily both push to the ledger. Queue them instead of racing.
concurrency:
  group: ledger
  cancel-in-progress: false

jobs:
  pulse:
    runs-on: ubuntu-latest
    # A job is killed at six hours anyway; an explicit ceiling stops one hung
    # request from burning an hour of wall clock.
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm

      - run: npm ci

      - name: Collect
        env:
          # Secret names cannot begin with GITHUB_, so the secret is stored
          # under SIGNAL_GITHUB_PAT and mapped here.
          #
          # This is a fine-grained token with public-repository read access, not
          # the built-in Actions token: that one is capped at 1,000/hour per
          # repository, which is too close to the working budget.
          GITHUB_PAT: ${{ secrets.SIGNAL_GITHUB_PAT }}
          # Passed as an environment variable, never interpolated into the
          # command text. An expression expanded into `run:` is substituted
          # before the shell ever sees it, so a crafted input would execute
          # rather than be read as an argument.
          LIMIT: ${{ inputs.limit }}
        run: |
          set -euo pipefail
          if [ -n "${LIMIT:-}" ]; then
            node scripts/pulse.ts --limit="${LIMIT}"
          else
            node scripts/pulse.ts
          fi

      - name: Summarise
        # Never blocks publishing. If Groq is down or out of quota, every event
        # still ships with its raw numbers and no prose — which is the honest
        # rendering anyway. Losing interpretation is not losing measurement.
        continue-on-error: true
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: node scripts/summarise.ts

      - name: Build bundles
        id: build
        env:
          # Public value, not a credential — it appears in the page source of
          # every site using it. Stored as a secret only so it is easy to set
          # once and forget; unset, no analytics script is emitted at all.
          CF_BEACON_TOKEN: ${{ secrets.CF_BEACON_TOKEN }}
        run: node scripts/build.ts

      - name: Deploy to Cloudflare Pages
        id: deploy
        # Static assets, plus one Pages Function. Every page and every bundle is
        # a file on the edge; the single dynamic route is /api/ask, which no
        # page depends on and which is compiled from functions/ by wrangler.
        if: steps.build.outputs.deploy == 'true'
        # wrangler is invoked directly rather than through cloudflare's action.
        # The action installs wrangler at runtime with npx, npm refuses to
        # install without a confirmation it cannot ask for in CI, and the
        # half-installed wrangler that runs anyway reports a missing API token —
        # which is a symptom, not the fault. `--yes` is the whole fix, and doing
        # it here removes a third-party action from a step that holds a
        # deploy token.
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx --yes wrangler@4 pages deploy dist --project-name=readout --branch=main

      - name: Record deployment
        # Advances the stored hash only on success, so a failed deploy leaves
        # the gate open instead of marking a bundle as shipped that never was.
        if: always() && steps.build.conclusion == 'success'
        run: |
          node scripts/record-deploy.ts \
            --hash='${{ steps.build.outputs.hash }}' \
            --deployed='${{ steps.deploy.conclusion == 'success' }}'

      - name: Commit ledger
        # Runs even when the collection step failed and even when nothing
        # changed. This is load-bearing: scheduled workflows are auto-disabled
        # after 60 days of repository inactivity, and the commit is what keeps
        # that from ever firing. Removing it fails silently, months later.
        if: always()
        run: |
          set -euo pipefail
          git config user.name  "readout-agent"
          git config user.email "readout-agent@users.noreply.github.com"
          git add data
          git commit --allow-empty -m "chore(data): pulse $(date -u +'%Y-%m-%dT%H:%MZ')"

          # The concurrency group serialises the scheduled writers, so the plain
          # push is the normal path. It does not serialise a person pushing by
          # hand, and that race lost a run's readings once: the fallback rebased
          # and the rebase conflicted on data/, because both sides had touched
          # it.
          #
          # Rebase is the wrong tool here. This run holds the newest reading of
          # every file it writes by construction — it just took them — so on a
          # race the answer is to take the remote history and re-apply this
          # run's data on top, not to replay a commit hunk by hunk.
          push_ledger() {
            git push origin "HEAD:${GITHUB_REF_NAME}" && return 0

            git fetch --unshallow --quiet origin "${GITHUB_REF_NAME}" \
              || git fetch --quiet origin "${GITHUB_REF_NAME}"

            # Keep the generated files, move onto the remote tip, put them back.
            local tree
            tree=$(mktemp -d)
            cp -r data "${tree}/"
            git reset --hard "origin/${GITHUB_REF_NAME}"
            # Overlay onto the remote tree rather than replacing it. See the
            # same step in daily.yml: deleting the directory first reverted
            # whatever the winner of the race had appended.
            cp -r "${tree}/data/." data/

            git add data
            git commit --allow-empty -m "chore(data): pulse $(date -u +'%Y-%m-%dT%H:%MZ')"
            git push origin "HEAD:${GITHUB_REF_NAME}"
          }

          push_ledger

      - name: Open an issue on failure
        # GitHub does not notify on scheduled workflow failure. Without this the
        # agent can be dead for weeks while the site quietly serves stale data.
        if: failure()
        uses: actions/github-script@v9
        with:
          script: |
            const title = 'Pulse failed';
            const existing = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'agent-failure',
            });
            const body = `Run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            if (existing.data.some((issue) => issue.title === title)) {
              const issue = existing.data.find((i) => i.title === title);
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                body,
              });
            } else {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels: ['agent-failure'],
              });
            }
